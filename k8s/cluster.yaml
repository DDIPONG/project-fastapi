apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: project-eks
  region: ap-northeast-2

managedNodeGroups:
  - name: project-nodegroup
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 1
    maxSize: 2
